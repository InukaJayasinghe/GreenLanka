name: green_lanka
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1  
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - images/sri_lankan_leopard.jpeg
    - images/deforestation_of_sri_lanka.jpeg
    - images/profile_picture.jpeg
    - images/map.png

flutter_icons:  # This should be OUTSIDE the "flutter:" section
  android: true
  ios: true
  image_path: "images/app_icon.png"
  # adaptive_icon_foreground: "images/app_icon_foreground.png"
  # adaptive_icon_background: "images/app_icon_background.png"
